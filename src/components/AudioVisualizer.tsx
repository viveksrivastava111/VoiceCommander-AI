export default function AudioVisualizer({levels,isActive}:{levels:number[];isActive:boolean}){return <div className="pointer-events-none absolute inset-[-30px] flex items-center justify-center">{levels.slice(0,24).map((v,i)=>{const a=i*15;return <span key={i} className="absolute w-1 rounded-full bg-brand-500 transition-all" style={{height:`${isActive?10+v*38:8}px`,transform:`rotate(${a}deg) translateY(-72px)`}}/>})}</div>}
