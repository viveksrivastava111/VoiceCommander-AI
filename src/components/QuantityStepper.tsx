import { Minus,Plus } from 'lucide-react'; export default function QuantityStepper({value,onChange}:{value:number;onChange:(n:number)=>void}){return <div className="flex items-center rounded-lg border border-gray-200"><button className="p-2" onClick={()=>onChange(Math.max(1,value-1))}><Minus size={15}/></button><span className="min-w-8 text-center text-sm font-semibold">{value}</span><button className="p-2" onClick={()=>onChange(value+1)}><Plus size={15}/></button></div>}
