import { Trash2 } from 'lucide-react'; import { CartItem as CI, Product } from '../types'; import QuantityStepper from './QuantityStepper'; import { money } from '../utils/speech';
export default function CartItem({item,product,onRemove,onQuantity}:{item:CI;product?:Product;onRemove:()=>void;onQuantity:(n:number)=>void}){return <div className="flex items-center gap-3 border-b border-gray-100 py-3 last:border-0"><img className="h-14 w-14 rounded-lg object-cover bg-gray-50" src={product?.image} alt=""/><div className="min-w-0 flex-1"><p className="truncate font-semibold">{item.name}</p><p className="text-sm text-gray-500">{money((product?.price||0)*item.quantity)} · {item.unit}</p></div><QuantityStepper value={item.quantity} onChange={onQuantity}/><button onClick={onRemove} className="p-2 text-gray-400 hover:text-red-500"><Trash2 size={18}/></button></div>}
